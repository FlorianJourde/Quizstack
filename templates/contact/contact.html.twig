{% extends 'layout.html.twig' %}

{% set title = 'Contact' %}

{% block meta_description %}Have questions about Quizstack ? Contact our team for support with your web development quiz challenges or account issues{% endblock %}

{% block body %}

    {{ include('partials/banner.html.twig') }}

    <div class="wrapper">
        <div class="page-header-container subtitle">
            <h1 class="big-title grow">Contact</h1>
            <h2 class="fade-in-up md:text-lg opacity-75" style="transition-delay: 0.2s;">
                Get in touch : ask a question, suggest a feature, or partner with Quizstack.
            </h2>
        </div>
    </div>

    <div class="wrapper wrapper-small">
        <div class="form-container glass box bg-dark-grey-secondary">

            {{ include('partials/form/errors.html.twig') }}

            {{ form_start(form) }}

            {% if form.email is defined %}
                <div>
                    <h2 class="small-title">
                        <span class="material-icons">alternate_email</span>
                        {{ form.email.vars.label }}
                    </h2>
                    {{ form_widget(form.email) }}
                    {{ form_errors(form.email) }}
                </div>
            {% endif %}
            <div>
                <h2 class="small-title">
                    <span class="material-icons">person</span>
                    {{ form.username.vars.label }}
                </h2>
                {{ form_widget(form.username) }}
                {{ form_errors(form.username) }}
            </div>
            <div>
                <h2 class="small-title">
                    <span class="material-icons">mail</span>
                    {{ form.message.vars.label }}
                </h2>
                {{ form_widget(form.message) }}
                {{ form_errors(form.message) }}
            </div>

            <div class="buttons-container">
                <a href="{{ path('app_home') }}" class="button button-tertiary">Cancel</a>
                <button type="submit" class="button button-primary">Send</button>
            </div>

            {{ form_end(form) }}

        </div>
    </div>

    <div class="wrapper">
        <div class="pt-spacing-very-large-tertiary flex flex-col gap-4">
            <p class="fade-in-up opacity-75" style="transition-delay: 0.2s;">Have a question, idea, or issue to report?
                Use the form below to reach the Quizstack team. We read every message carefully and typically reply
                within 1–2 business days. To help us assist you faster, include as much context as possible - page URLs,
                screenshots, device/browser details, and steps to reproduce if you’re reporting a bug.</p>
            <p class="fade-in-up opacity-75" style="transition-delay: 0.2s;">Product suggestions are welcome. Tell us
                which quiz topics you’d like to see next, how we can improve explanations, or which features would make
                your learning experience smoother. Educators, companies, and community organizers can also contact us
                about partnerships, bulk access, or custom leaderboards for events and classrooms.</p>
            <p class="fade-in-up opacity-75" style="transition-delay: 0.2s;">We value respectful, clear communication.
                Please avoid sharing sensitive credentials; if needed, we’ll arrange a secure follow-up. By submitting
                the form, you agree to our privacy policy and understand that we’ll use your information to respond to
                your request. Thank you for helping us build a better web development learning platform.</p>
        </div>
    </div>

{% endblock %}

