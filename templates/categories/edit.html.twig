{% extends 'base.html.twig' %}

{% block title %}Edit categories{% endblock %}


{% block body %}

    {{ include('partials/banner.html.twig') }}


    <div class="wrapper">
        <div class="page-header-container">
            <h1 class="big-title grow">Edit categories</h1>
            {#            <div class="buttons-container flex gap-2 justify-end"> #}
            {#                <a class="button button-action" href="{{ path('question_edit', {'id': question.id}) }}"> #}
            {#                    #}{#                Edit #}
            {#                    <span class="material-icons">edit</span> #}

            {#                </a> #}
            {#            </div> #}
        </div>
    </div>

    {#    <div class="container mt-4"> #}
    <div class="wrapper">


        {#        {% for message in app.flashes('success') %} #}
        {#            <div class="alert alert-success"> #}
        {#                {{ message }} #}
        {#            </div> #}
        {#        {% endfor %} #}

        {# Form to add a new category #}
        {#        <div class="card mb-4"> #}
        <div class="form-container glass box bg-dark-grey-secondary">
            <h2 class="small-title">Add category</h2>


            {#            <div class="card-body"> #}
            {{ form_start(addForm) }}
            {#                <div class="row align-items-end"> #}
            {#                    <div class="col-md-8"> #}
            {#                    {{ form_label(addForm.name) }} #}
            {#                    {{ form_label(addForm.color) }} #}
            {#                        {{ form_label(addForm.name, 'Category Name') }} #}
            {#                        {{ form_widget(addForm.name, {'attr': {'placeholder': 'Enter new category name'}}) }} #}
            {#                    {{ form_row(addForm.name) }} #}
            {#                    {{ form_row(addForm.color) }} #}

            <div>
                <h2 class="small-title">{{ addForm.name.vars.label }}</h2>
                {{ form_widget(addForm.name) }}
            </div>
            <div>
                <h2 class="small-title">{{ addForm.color.vars.label }}</h2>
                {{ form_widget(addForm.color) }}
            </div>

            {#                    </div> #}
            {#                    <div class="col-md-2"> #}
            {#                        <div class="form-check"> #}
            {#                            {{ form_widget(addForm.status) }} #}
            {#                            <label class="form-check-label" for="{{ addForm.status.vars.id }}">Enabled</label> #}
            {#                        </div> #}
            {#                    </div> #}
            {#                    <div class="col-md-2"> #}
            {#                        <button type="submit" class="button button-success w-100">Add</button> #}

            <div class="buttons-container flex mb-4 gap-4 justify-end">
                <button type="submit" class="button button-primary">Add</button>


                {#                        <a href="{{ path('questions') }}" class="button button-tertiary">Cancel</a> #}
                {#                        {{ form_row(form.delete) }} #}
                {#                        <button class="button button-primary" type="submit">Update</button> #}
            </div>

            {#                    </div> #}
            {#                    {% if category.id %} #}
            {#                        {{ form_widget(categoryField.delete) }} #}
            {#                    {% endif %} #}
            {{ form_end(addForm) }}
        </div>
        {#    </div> #}
        {#        </div> #}

{#    </div>#}
{#    <div class="wrapper">#}

        <div class="form-container glass box bg-dark-grey-secondary">

            <h2 class="small-title">Edit categories</h2>

            {# Form to edit all categories #}
            {{ form_start(editForm) }}
            {#            <div class="card"> #}
            {#                <div class="card-header d-flex justify-content-between align-items-center"> #}
            {#                    <h5 class="mb-0">Edit Categories</h5> #}

            {#                </div> #}
            {#                <div class="card-body"> #}
            <div class="overflow-auto">
                <table class="table glass">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Color</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for categoryField in editForm.categories %}
                        {% set category = categoryField.vars.data %}
                        <tr>
                            <td>{{ category.id }}</td>
                            {#                                    <td class="text-center"> #}
                            {#                                        {{ category.status ? 'online' : 'offline' }} #}
                            {#                                    </td> #}


                            <td>{{ form_widget(categoryField.name) }}</td>
                            <td>{{ form_widget(categoryField.color) }}</td>

                            <td>
                                <span class="text-xs rounded-sm px-2 py-1 {{ category.status ? 'bg-green-secondary/50' : 'bg-red-secondary/50' }}">
                                    {{ category.status ? 'online' : 'offline' }}
                                </span>
                            </td>

                            <td>
                                <div class="button-group">
                                    <a href="{{ path('category_toggle', {'id': category.id}) }}"
                                       class="button button-sm button-outline-{{ category.status ? 'warning' : 'success' }}">
                                        {{ category.status ? 'Disable' : 'Enable' }}
                                    </a>

                                    {#                                        <a href="{{ path('category_delete', {'id': category.id}) }}" #}
                                    {#                                           class="btn btn-sm btn-outline-{{ category.status ? 'warning' : 'success' }}"> #}
                                    {#                                            {{ category.status ? 'Disable' : 'Enable' }} #}
                                    {#                                            Delete #}
                                    {#                                        </a> #}

                                    {#                                        {% if categoryField.delete is defined %} #}
                                    {{ form_widget(categoryField.delete) }}
                                    {#                                        {% endif %} #}

                                    {#                                        {% if category.id %} #}
                                    {#                                            {{ form_widget(categoryField.delete) }} #}
                                    {#                                        {% endif %} #}

                                    {#                                        {{ form_widget(categoryField.delete) }} #}

                                    {#                                        <form method="post" action="{{ path('category_delete', {'id': category.id}) }}" #}
                                    {#                                              onsubmit="return confirm('Are you sure you want to delete this category?');"> #}
                                    {#                                            <input type="hidden" name="_token" #}
                                    {#                                                   value="{{ csrf_token('delete' ~ category.id) }}"> #}
                                    {#                                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button> #}
                                    {#                                        </form> #}
                                    {#                                        <a href="#" #}
                                    {#                                           class="btn btn-sm btn-outline-danger delete-button" #}
                                    {#                                           data-id="{{ category.id }}" #}
                                    {#                                           data-token="{{ csrf_token('delete' ~ category.id) }}"> #}
                                    {#                                            Delete #}
                                    {#                                        </a> #}
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center">No categories found.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            {#                </div> #}
            {#            {{ form_widget(editForm.save) }} #}

            <div class="buttons-container flex mb-4 gap-4 justify-end">

                {{ form_widget(editForm.save) }}


                {#                <a href="{{ path('questions') }}" class="button button-tertiary">Cancel</a> #}
                {#                {{ form_row(form.delete) }} #}
                {#                <button class="button button-primary" type="submit">Update</button> #}
            </div>


            {#            </div> #}
            {{ form_end(editForm) }}
        </div>

        {#        {% for categoryField in editForm.categories %} #}
        {#            {% set category = categoryField.vars.data %} #}
        {#            <form id="delete-form-{{ category.id }}" method="post" #}
        {#                  action="{{ path('category_delete', {'id': category.id}) }}" style="display: none;"> #}
        {#                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}"> #}
        {#                <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button> #}
        {#            </form> #}
        {#        {% endfor %} #}

    </div>
    {#    </div> #}
{% endblock %}

{# {% block body %} #}


{#    <div class="categories-list"> #}
{#        <table class="table table-striped"> #}
{#            <thead> #}
{#            <tr> #}
{#                <th>ID</th> #}
{#                <th>Status</th> #}
{#                <th>Name</th> #}
{#            </tr> #}
{#            </thead> #}
{#            <tbody> #}
{#            {% for category in categories %} #}
{#                <tr> #}
{#                    <td>{{ category.id }}</td> #}
{#                    <td>{{ category.status ? 'online' : 'offline' }}</td> #}
{#                    <td>{{ category.name }}</td> #}
{#                </tr> #}
{#            {% else %} #}
{#                <tr> #}
{#                    <td colspan="5" class="text-center">No categories.</td> #}
{#                </tr> #}
{#            {% endfor %} #}
{#            </tbody> #}
{#        </table> #}
{#    </div> #}

{#    #}{#    <div class="container mt-4"> #}
{#    #}{#        <h1>Add new question</h1> #}

{#    #}{#        {{ form_start(form) }} #}
{#    #}{#        <div class="row"> #}
{#    #}{#            <div class="col-md-8"> #}
{#    #}{#                {{ form_row(form.content) }} #}
{#    #}{#                {{ form_row(form.explanation) }} #}
{#    #}{#            </div> #}
{#    #}{#            <div class="col-md-4"> #}
{#    #}{#                {{ form_row(form.difficulty) }} #}
{#    #}{#                {{ form_row(form.categories) }} #}
{#    #}{#            </div> #}
{#    #}{#        </div> #}

{#    #}{#        <div class="d-flex justify-content-between mt-3"> #}
{#    #}{#            <a href="{{ path('question_index') }}" class="btn btn-secondary">Annuler</a> #}
{#    #}{#            <button type="submit" class="btn btn-success">Créer</button> #}
{#    #}{#        </div> #}
{#    #}{#        {{ form_end(form) }} #}
{#    #}{#    </div> #}
{# {% endblock %} #}